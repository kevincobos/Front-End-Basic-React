function UsingProps(props) {
    return (
        <div className="card">
            <h1>Using props from UsingProps, {props.textShowTop}, {props.textShowBottom}</h1>
        </div>
    );
  };
export default UsingProps;